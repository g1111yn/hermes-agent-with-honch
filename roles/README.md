# Role Bundles

Each role bundle is portable and importable into Hermes profiles.

Required files:

- `SOUL.md`
- `memories/USER.md`
- `memories/MEMORY.md`
- `presence-config.json`

Optional:

- `skills/`

Import with:

```bash
./scripts/import_role_bundle.sh <role-id>
```
